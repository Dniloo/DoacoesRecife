<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.doarecife.doacoesrecife">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".DoacaoAPP"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DoacoesActivity"
            android:theme="@style/AppTheme.NoTitle"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DetalheDoacaoActivity"
            android:label="@string/detalhe_local_titulo"
            android:parentActivityName=".DoacoesActivity">
            <meta-data android:name="parentActivityName"
                android:value=".DoacoesActivity"/>
        </activity>
    </application>

</manifest>